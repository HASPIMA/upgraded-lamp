generator client {
  provider             = "prisma-client-py"
  recursive_type_depth = "10"
  interface            = "asyncio"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
